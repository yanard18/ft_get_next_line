## Static Variables

Static variable only initialized once, so they keep the value after function calls return.

## BUFFER\_SIZE

BUFFER\_SIZE increase the efficency of IO operations by reducing the number of system\_calls.

[TODO] add debug analysis of read()
